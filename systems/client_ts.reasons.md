# client_ts — reasons

The reason behind each rule in `systems/client_ts.md`, keyed by the rule's id (orchestration's DESIGN §4). Read one with `pipeline reasons client_ts#n` before changing the rule it belongs to.
