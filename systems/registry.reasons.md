# registry — reasons

The reason behind each rule in `systems/registry.md`, keyed by the rule's id (orchestration's DESIGN §4). Read one with `pipeline reasons registry#n` before changing the rule it belongs to.
