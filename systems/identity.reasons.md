# identity — reasons

The reason behind each rule in `systems/identity.md`, keyed by the rule's id (orchestration's DESIGN §4). Read one with `pipeline reasons identity#n` before changing the rule it belongs to.
