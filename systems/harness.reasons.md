# harness — reasons

The reason behind each rule in `systems/harness.md`, keyed by the rule's id (orchestration's DESIGN §4). Read one with `pipeline reasons harness#n` before changing the rule it belongs to.
