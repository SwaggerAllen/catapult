<?xml version="1.0" encoding="UTF-8"?>
<!-- The bug_fix flow's planning-tier grammar. No siege source (v4
     §8.2's prose is the only input) — kept small. -->
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema">
  <xs:element name="bug-fix-plan">
    <xs:complexType>
      <xs:sequence>
        <!-- The ticket surface's own headline (ORC-114, `chain.md` #38's
             reserved `argument` fields: name) — one or two sentences,
             why this fix matters, not a restatement of <observed>/<expected>. -->
        <xs:element name="argument" type="xs:string"/>
        <xs:element name="observed" type="xs:string"/>
        <xs:element name="expected" type="xs:string"/>
        <xs:element name="scope" type="xs:string"/>
        <xs:element name="resolved" type="xs:boolean"/>
      </xs:sequence>
    </xs:complexType>
  </xs:element>
</xs:schema>
