{% render "partials/_review_framing", draft: draft, prior_review: prior_review %}

Checklist for this tier:

- Does `<title>` name what this reference actually covers, specifically enough that an attaching agent can tell without opening the body?
- Does `<body>` stay faithful to the seed, or does it assert specifics (versions, numbers, decisions) the seed never gave?
- Does every `<see-also>` target point at a reference the seed actually named, not an invented id?
