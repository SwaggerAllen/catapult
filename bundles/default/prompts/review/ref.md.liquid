{% render "partials/_review_framing" %}

# Draft

{{ draft }}

# Checklist

- Is this genuinely implementation detail (runbook, spec, technology
  guide), or does it actually restate an architectural decision that
  belongs in sysarch/comparch instead?
- Is the title specific enough that a downstream author scanning a
  list of attached refs would recognize this one without opening it?
- If expanded from a seed, does the body stay within what the seed
  actually implied, or did the expansion invent scope?
- Do `<see-also>` references point at refs that actually exist?
