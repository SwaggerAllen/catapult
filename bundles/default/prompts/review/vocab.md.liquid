{% render "partials/_review_framing", draft: draft, prior_review: prior_review %}

Checklist for this tier:

- Does `<definition>` give this term's project-specific meaning, or could it describe the word's dictionary sense on any project?
- Is `<disambiguation>` present when the term could be misread against common usage or a nearby project term, and absent (not padded with filler) when it wouldn't be?
- Does every `<see-also>` `<ref name="...">` point at a term this entry actually depends on or is routinely confused with, not an unrelated pointer?
