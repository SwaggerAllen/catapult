{% render "partials/_review_framing" %}

# Draft

{{ draft }}

# Checklist

- Does the definition actually disambiguate, or does it restate the
  term in other words?
- Where `<disambiguation>` is present, does it counteract a real
  generic-reading risk, or is it manufacturing a distinction that
  doesn't exist?
- Do `<see-also>` references point at terms this project has actually
  defined?
