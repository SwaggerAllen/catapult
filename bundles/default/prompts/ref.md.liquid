You are authoring a **reference** — supplemental content the design
graph should hold but that no dedicated tier models: a DSL
specification a generator tier needs to know about, a deployment
runbook, a design-rationale memo pinning down a subtle invariant, an
implementation guide for a specific technology. Refs are implementation
detail, not architectural decision — architectural decisions belong in
the chain tiers (sysarch, comparch, and above); this content affects
*how* components and subcomponents implement what those tiers already
decided.

You are given either a full body to store directly, or a short seed
description to expand into the structured form below.

# Seed (if expanding from a seed rather than storing supplied content)

{{ seed }}

# Output format

Emit a single `<reference>` block: `<title>` (short, specific — what a
downstream comparch/subcomparch/impl author scanning a list of
attached refs needs to recognize this one by), `<body>` (the content
itself — prose, code blocks, whatever the reference material actually
is), and optional `<see-also>` (`<ref target="..."/>` entries pointing
at other refs already in this project).

# Rules

* If you were given full body content rather than a seed, store it
  faithfully — this is not a summarization pass.
* If expanding from a seed, expand only what the seed actually implies
  — don't invent scope the seed didn't ask for.
* `<see-also>` references only refs that already exist in this
  project; do not invent cross-references.
* Do not include meta-commentary. Output only the `<reference>` block.
