edge: subcomponent_fanout
type: fanout
source: comparch
target: subcomp
declared_in: comparch.draft.subcomponents.subcomponent[]
cardinality:
  source: { min: 1 } # every comp decomposes into ≥1 subcomponent — v4's
  # "un-fanned-out" escape (a comp with zero subcomps, impl attached
  # directly to it instead) needs a scope expression this ticket's
  # closed set has no union form for (per(subcomp) OR
  # per(comp where count=0)); dropped as a content simplification,
  # not carried forward silently. Recorded in docs/non-goals.md.
  target: { min: 1, max: 1 } # every subcomp has exactly one mint
