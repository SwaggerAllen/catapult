depth: [2, 0]  # opt-in: this file's presence is what turns the critique
               # slot on (dsl-syntax.md §15.5). 2 for the project's first
               # traversal — the architecture chain's own deepest fan-out
               # is two edges deep (sysarch's decomposition into comp,
               # then comparch's decomposition into subcomp — one edge
               # name, two instances, edges/decomposition.yaml), so 2
               # reviews both levels a first pass produces; 0 for every
               # later traversal, which returns to the top level once a
               # human or the auto-reviewer has already read the graph
               # once (v5 §7.19).
