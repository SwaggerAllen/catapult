review: engineering-review
role: engineering
depth: 0
throwback: ux-review               # one target, not a list (§15.10): a
                                    # decline lands on exactly one status,
                                    # so the field names a landing point
                                    # rather than bounding a set. It kept
                                    # `ux-review` over `generation` as an
                                    # ordinary bundle-authoring call, not
                                    # because `generation` restates
                                    # anything: since the fourth-pass
                                    # correction, this gate's derived
                                    # default is the sub-array's own
                                    # leading `pending`, not `generation`
                                    # itself, so `generation` is a genuine
                                    # landing point this single-valued
                                    # field just doesn't happen to name.
                                    # Both stay legal either way: legality
                                    # is "earlier in the citing type's
                                    # effective sequence" and never a
                                    # per-gate list (Catapult.Dsl.Workflow
                                    # .throwback_targets/3)
escalation: author
