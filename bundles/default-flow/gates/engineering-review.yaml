review: engineering-review
role: engineering
depth: 0
throwback: [generation, ux-review] # both sit earlier than this gate in
                                    # types/feature.yaml's own array — a
                                    # display label like "Architecting" is
                                    # not declarable vocabulary and fails
                                    # load (Catapult.Dsl.Workflow's
                                    # gate_throwback_problems/2)
escalation: author
