review: milestone-signoff
role: author                       # the author's own manual pass across
                                    #   the milestone — v5 §7.8 names this
                                    #   irreducibly human, and DESIGN §10
                                    #   names it the only place manual
                                    #   testing happens
depth: 0
throwback: [main]                  # §15.8's own worked case: "a milestone
                                    #   sign-off gate between `main` and
                                    #   `retro` rejecting back to `main` is
                                    #   an ordinary throwback"
escalation: author
