# Signpost — mocks

No design-system mocks or wireframes exist for this toy project —
Signpost is a redirect service with no first-party UI beyond a plain
link-creation form, and that form is out of this seed's depth (v5
§4.1/§5.4 seed evidence).

This document stands in for the `mocks` input role so the role has a
real file to resolve against once intake/raft storage exists (Phase
4); it deliberately carries no visual content to pin against.
